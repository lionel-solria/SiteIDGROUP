# Migration SEO ID GROUP

Ce dossier prépare la cartographie des redirections 301 pour une future mise en production. Complétez `redirect-map.csv` avec les anciennes URL et leurs équivalents sur le nouveau site statique.

- Garder les URLs propres (paramètre `?lang=` pour la langue si besoin)
- Vérifier les metas title/description sur chaque page
- Mettre à jour le sitemap.xml une fois les URLs finales validées
