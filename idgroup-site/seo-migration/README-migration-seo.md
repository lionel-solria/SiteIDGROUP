# Migration SEO – ID GROUP (statique)

Ce dossier prépare la phase de migration 301 depuis l'existant vers le nouveau site statique. Aucun outil serveur n'est requis ici, uniquement la documentation pour l'équipe SEO/ops.

- `redirect-map.csv` : à compléter avec les anciennes URLs et les nouvelles destinations (format `from,to`).
- Penser aux paramètres `?lang=fr|en|de` utilisés pour l'i18n sans serveur.
- Les pages livrées en phase 1 : index, solutions, univers (home/pro/agri), le groupe, RSE, actualités, article (template), contact, espace-client.
- Robots et sitemap statiques sont présents à la racine du dossier.
